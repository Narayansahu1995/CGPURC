@model Private_University.Models.FeesReceived
@using Private_University.Models;

@{
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<!DOCTYPE html>

@*<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>FeesReceived</title>
</head>
<body>*@
<div class="sb2-2-2">
    <ul>       
        <li class="page-back">
            <a href="~/FeesCollect/FeesReport"><i class="fa fa-backward" aria-hidden="true"></i>Back to Fees Report</a>
        </li>
    </ul>
</div>

<div class="sb2-2-3">
    <div class="row">
        <div class="col-md-6">
            <div class="box-inn-sp admin-form">
                <div class="inn-title">
                    <h4>Fees Collection</h4>
                </div>
                <div class="tab-inn">

                    @*@using (Html.BeginForm("FeesReceived", "FeesCollect", FormMethod.Post, new { Class = "s12" }))*@
                    @using (Html.BeginForm("FeesReceived", "FeesCollect", FormMethod.Post))
                    {
                        @Html.AntiForgeryToken()
                        AuthenticationResponse ARespo = (AuthenticationResponse)Session["AuthResponse"];

                        <div class="form-horizontal">
                            <div class="form-group">
                                @Html.LabelFor(model => model.NoOfStudents, htmlAttributes: new { @class = "control-label col-md-5" })
                                <div class="col-md-5">
                                    @Html.EditorFor(model => model.NoOfStudents, new { htmlAttributes = new { @class = "form-control", min = "1" } })
                                    @Html.ValidationMessageFor(model => model.NoOfStudents, "", new { @class = "text-danger" })
                                </div>
                            </div>
                            <div class="form-group">
                                @Html.LabelFor(model => model.FeesAmount, htmlAttributes: new { @class = "control-label col-md-5" })
                                <div class="col-md-5">
                                    @Html.EditorFor(model => model.FeesAmount, new { htmlAttributes = new { @class = "form-control", min="1" } })
                                    @Html.ValidationMessageFor(model => model.FeesAmount, "", new { @class = "text-danger" })
                                </div>
                            </div>
                            <div class="form-group">
                                @Html.LabelFor(model => model.ReceivedDate, htmlAttributes: new { @class = "control-label col-md-5" })
                                <div class="col-md-5">                                    
                                    @*@Html.TextBoxFor(model => model.ReceivedDate, DateTime.Now.ToString("yyyy-MM-dd"), new { type = "date", @Value = DateTime.Now.ToString("yyyy-MM-dd"), max = DateTime.Now.ToString("yyyy-MM-dd"), min = DateTime.Now.AddMonths(-1).ToString("yyyy-MM-01") })*@
                                    @Html.EditorFor(model => model.ReceivedDate, new { htmlAttributes = new { @class = "form-control", @Value = @DateTime.Now.ToString("yyyy-MM-dd"), max = DateTime.Now.ToString("yyyy-MM-dd"), min = DateTime.Now.AddMonths(-1).ToString("yyyy-MM-01") } })
                                   
                                    @Html.ValidationMessageFor(model => model.ReceivedDate, "", new { @class = "text-danger" })
                                </div>
                            </div>
                            @Html.HiddenFor(model => model.University_ID, new { Value = ARespo.University_ID })
                            @Html.HiddenFor(model => model.Session_ID, new { Value = ARespo.Session_ID })

                            <div class="form-group">
                                <div class="col-md-offset-6 col-md-6">
                                    <input type="submit" value="Save" class="btn btn-primary" />
                                </div>
                            </div>
                            @Html.Raw(TempData["msg"])
                        </div>

                    }
                        </div>
                </div>
            </div>
        </div>
    </div>

   

  
    @*</body>
        </html>*@
